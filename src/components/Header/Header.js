import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Clicky Game!</h1>
    <h2>
      Click on an image to score, but don't click on any picture twice!
    </h2>
  </header>
);

export default Header;
